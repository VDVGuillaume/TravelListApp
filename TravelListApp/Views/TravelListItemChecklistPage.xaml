<Page
    x:Class="TravelListApp.Views.TravelListItemChecklistPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TravelListApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"    
    xmlns:uc="using:TravelListApp.UserControls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="Grid" Background="{StaticResource TravelBackground2}"
          Padding="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <local:TravelListItemMenu x:Name="Menu" />

        <Grid Grid.Column="0" Grid.Row="1" VerticalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="11*" />
                <ColumnDefinition Width="63*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Checklist Editor"
                            Foreground="{StaticResource WhiteBrush}"
                            FontSize="24"
                            Margin="20 20 0 10"
                            Grid.ColumnSpan="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />

            <Button         Content="Add New Item"
                             BorderThickness="0.5"
                             Foreground="{StaticResource WhiteBrush}"
                             BorderBrush="{StaticResource WhiteBrush}"                                                          
                             Margin="0,10,25,0"
                             Grid.Row="1"
                             Grid.Column="0" HorizontalAlignment="Right" Height="29" VerticalAlignment="Center" Width="106" />

            <TextBox        PlaceholderText="name the item to be added"
                             BorderThickness="0.5"
                             Foreground="{StaticResource WhiteBrush}"
                             BorderBrush="{StaticResource WhiteBrush}"
                             Background="{StaticResource transBrush}"
                             Text="{x:Bind ViewModel.Description, Mode=TwoWay}"
                             Margin="10,10,20,0"
                             Grid.Row="1"
                             Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />


            <ListBox 
                            HorizontalAlignment="Stretch" 
                            Height="100" 
                            Margin="10,20,20,0" 
                            Background="Transparent"
                            Grid.Row="2" 
                            Grid.Column="1"
                            VerticalAlignment="Stretch"/>

        </Grid>
        




    </Grid>
</Page>
